{
  "name": "freetato",
  "version": "0.0.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "MIT",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -p .",
    "build": "cp src/*.css build/src/ && cp src/*.html build/src/"
  },
  "devDependencies": {
    "typescript": "~3.4.0"
  }
}
